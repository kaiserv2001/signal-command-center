# Leave blank for local dev — the Vite proxy forwards /api and /socket.io to the
# server, so requests stay same-origin (no CORS). In production set this to the
# deployed Render API origin, e.g. https://signal-api.onrender.com
VITE_API_URL=
