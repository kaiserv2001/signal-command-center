PORT=4000
# Paste your MongoDB Atlas M0 connection string here (leave blank to run without DB)
MONGODB_URI=
JWT_SECRET=change-me-in-prod
# Allowed browser origin(s) for CORS + Socket.io. Single origin in dev; in prod
# set to your Vercel URL. Accepts a comma-separated list (prod + preview URLs):
# CLIENT_ORIGIN=https://signal.vercel.app,https://signal-git-main.vercel.app
CLIENT_ORIGIN=http://localhost:5173
